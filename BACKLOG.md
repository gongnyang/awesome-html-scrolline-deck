# Backlog

## 0.2 — motion variety (next)

The four example decks differ in concept, palette and pictures, but their choreography is the same twelve templates reused. Swapping pictures is what slide templates do; a scroll deck has to vary the *motion grammar*.

- Give every technique a `variants[]` axis in `template.json` (entrance texture, hold micro-motion, exit) and a `variant` field in `deck.json`.
- New techniques: split-screen compare, mask-reveal, typo-scramble, chart-draw, timeline-rail, zoom-into-detail, stack-collapse, orbit-3d, morph-shape, particle-text.
- Gate G11: no effect (technique + variant) used twice in one deck.
- Reassign the four example decks so no effect repeats across them; regenerate captures, the demo trailer and the handbook gallery.
